// Barrel export for all client-side data hooks.
//
// Convention: components import from '@/lib/api/hooks', not the individual
// files. The barrel is the seam between API contract and UI.

export * from './me';
